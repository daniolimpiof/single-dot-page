import { clickArrowButton } from "./Scripts/dropdownLogic.js";

const arrowButton = document.querySelector(".dropdown-gallery");
arrowButton.addEventListener("click", clickArrowButton);

import { circleLogicClicks } from "./Scripts/cicleLogic.js";

const circleButton = document.querySelector(".all-interactive");
circleButton.addEventListener("click", circleLogicClicks);